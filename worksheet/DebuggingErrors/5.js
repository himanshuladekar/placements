// Error-Based Debugging:

// Identify why the following function outputs undefined and fix it:

console.log(name);
var name = "JavaScript";

// it is undefined here so to make it correct we can assign it before and then printing or consoling


var name = "JavaScript";
console.log(name);