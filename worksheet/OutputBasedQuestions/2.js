const multiply = (a, b) => { a * b };
console.log(multiply(2, 3)); // Expected Output: 6

//in above question return statement is missing there

const multiply = (a, b) => { 
    return a * b;
 };
console.log(multiply(2, 3)); // Expected Output: 6

